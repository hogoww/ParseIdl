#ifndef __INVALID_CONNECTION_H__
#define __INVALID_CONNECTION_H__

#include <string>
#include <exception>
#include "Component.h"

/* class  Component::InvalidConnection : public std::exception{
 private:
  std::string errormsg;
  
 public:
  InvalidConnection(std::string ComponentType,std::string typePort);
  virtual ~InvalidConnection() throw();
  virtual const char* what() const throw();

};
*/
#endif
